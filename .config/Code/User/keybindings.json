// Place your key bindings in this file to override the defaults
[
    {
        "key": "ctrl+k",
        "command": "workbench.action.openView"
    },
    {
        "key": "space space",
        "command": "workbench.action.quickOpen",
        "when": "!inputBoxFocus && !terminalFocus && (vim.mode == 'Normal' || vim.mode == 'Visual')",
    }
]